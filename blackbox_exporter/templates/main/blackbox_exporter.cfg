{{ansible_managed|comment(decoration='#')}}

{% if (blackbox_exporter_state is match('start|on|enable')) %}
check process blackbox_exporter matching "/usr/local/bin/blackbox_exporter.*"
  start program = "/usr/bin/systemctl start blackbox_exporter"
  stop program = "/usr/bin/systemctl stop blackbox_exporter"
  if failed host 127.0.0.1 port 9115 type tcp with timeout 10 seconds then alert
  if 5 restarts within 5 cycles then unmonitor
{% endif %}
