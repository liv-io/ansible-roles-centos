---

- name: 'task include install'
  import_tasks: install.yml
  when: not (traefik_state is match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - traefik

- name: 'task include enable'
  import_tasks: enable.yml
  when: (traefik_state is match('start|on|enable'))
  tags:
    - main
    - traefik

- name: 'task include disable'
  import_tasks: disable.yml
  when: (traefik_state is match('stop|off|disable'))
  tags:
    - main
    - traefik

- name: 'task include remove'
  import_tasks: remove.yml
  when: (traefik_state is match('false|no|remove'))
  tags:
    - main
    - traefik
