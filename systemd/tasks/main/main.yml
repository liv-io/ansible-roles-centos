---

- name: 'task include install'
  include: install.yml
  when: not (systemd_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - systemd

- name: 'task include config'
  include: config.yml
  when: not (systemd_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - systemd
