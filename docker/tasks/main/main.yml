---

- name: 'task include install'
  import_tasks: install.yml
  when: not (docker_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - docker

- name: 'task include enable'
  import_tasks: enable.yml
  when: (docker_state|match('start|on|enable'))
  tags:
    - main
    - docker

- name: 'task include disable'
  import_tasks: disable.yml
  when: (docker_state|match('stop|off|disable'))
  tags:
    - main
    - docker

- name: 'task include remove'
  import_tasks: remove.yml
  when: (docker_state|match('false|no|remove'))
  tags:
    - main
    - docker
