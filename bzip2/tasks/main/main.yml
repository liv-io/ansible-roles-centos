---

- name: 'task include install'
  include: install.yml
  when: not (bzip2_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - bzip2

- name: 'task include remove'
  include: remove.yml
  when: (bzip2_state|match('false|no|remove'))
  tags:
    - main
    - bzip2
