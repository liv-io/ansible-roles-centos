---

#-------------------------------------------------------------------------------
# VARIABLES
#-------------------------------------------------------------------------------

- name: 'include variables'
  include_vars: "{{item}}"
  with_first_found:
    - files:
      - "vars/{{ansible_distribution_major_version}}.yml"
      - 'vars/main.yml'
      skip: 'true'
  tags:
    - remove
    - main
    - iptables

#-------------------------------------------------------------------------------
# PACKAGING
#-------------------------------------------------------------------------------

- name: 'package remove iptables-services'
  package:
    state: 'absent'
    name: "{{package_name_iptables_services}}"
  tags:
    - remove
    - main
    - iptables

- name: 'package remove iptables-utils'
  package:
    state: 'absent'
    name: "{{package_name_iptables_utils}}"
  tags:
    - remove
    - main
    - iptables

#-------------------------------------------------------------------------------
# FILES
#-------------------------------------------------------------------------------

- name: 'file remove ip6tables-config'
  file:
    state: 'absent'
    path: "{{file_path_ip6tables_config}}"
  tags:
    - remove
    - main
    - iptables

- name: 'file remove ip6tables.d'
  file:
    state: 'absent'
    path: "{{file_path_ip6tables_d}}"
  tags:
    - remove
    - main
    - iptables

- name: 'file remove iptables-config'
  file:
    state: 'absent'
    path: "{{file_path_iptables_config}}"
  tags:
    - remove
    - main
    - iptables

- name: 'file remove iptables.d'
  file:
    state: 'absent'
    path: "{{file_path_iptables_d}}"
  tags:
    - remove
    - main
    - iptables

- name: 'file remove ipv4-filter_ansible'
  file:
    state: 'absent'
    path: "{{file_path_ipv4_filter_ansible}}"
  tags:
    - remove
    - main
    - iptables

- name: 'file remove ipv4-nat_ansible'
  file:
    state: 'absent'
    path: "{{file_path_ipv4_nat_ansible}}"
  tags:
    - remove
    - main
    - iptables

- name: 'file remove ipv6-filter_ansible'
  file:
    state: 'absent'
    path: "{{file_path_ipv6_filter_ansible}}"
  tags:
    - remove
    - main
    - iptables

- name: 'file remove ipv6-nat_ansible'
  file:
    state: 'absent'
    path: "{{file_path_ipv6_nat_ansible}}"
  tags:
    - remove
    - main
    - iptables
