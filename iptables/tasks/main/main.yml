---

- name: 'task include install'
  include: install.yml
  when: not (iptables_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - iptables

- name: 'task include enable'
  include: enable.yml
  when: (iptables_state|match('start|on|enable'))
  tags:
    - main
    - iptables

- name: 'task include disable'
  include: disable.yml
  when: (iptables_state|match('stop|off|disable'))
  tags:
    - main
    - iptables

- name: 'task include remove'
  include: remove.yml
  when: (iptables_state|match('false|no|remove'))
  tags:
    - main
    - iptables
