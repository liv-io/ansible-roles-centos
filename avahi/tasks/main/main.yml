---

- name: 'task include install'
  include: install.yml
  when: not (avahi_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - avahi

- name: 'task include remove'
  include: remove.yml
  when: (avahi_state|match('false|no|remove'))
  tags:
    - main
    - avahi
