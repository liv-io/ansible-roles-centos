---

- name: 'task include install'
  include: install.yml
  when: not (yum_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - yum

- name: 'task include config'
  include: config.yml
  when: not (yum_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - yum
