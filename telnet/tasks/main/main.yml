---

- name: 'task include install'
  include: install.yml
  when: not (telnet_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - telnet

- name: 'task include remove'
  include: remove.yml
  when: (telnet_state|match('false|no|remove'))
  tags:
    - main
    - telnet
