---

#-------------------------------------------------------------------------------
# VARIABLES
#-------------------------------------------------------------------------------

- name: 'include variables'
  include_vars: "{{lookup('first_found', params)}}"
  vars:
    params:
      files:
        - "vars/{{ansible_distribution_major_version}}.yml"
        - 'vars/main.yml'
  tags:
    - cronjob
    - ntpd

#-------------------------------------------------------------------------------
# SYSTEM
#-------------------------------------------------------------------------------

- name: 'service stop ntpd'
  service:
    state: 'stopped'
    enabled: 'no'
    name: "{{service_name_ntpd}}"
  tags:
    - cronjob
    - ntpd

#-------------------------------------------------------------------------------
# FILES
#-------------------------------------------------------------------------------

- name: 'file create ntp.conf'
  template:
    src: "{{file_src_ntp_conf}}"
    dest: "{{file_dst_ntp_conf}}"
    mode: "{{file_mode_ntp_conf}}"
    owner: "{{file_owner_ntp_conf}}"
    group: "{{file_group_ntp_conf}}"
  tags:
    - cronjob
    - ntpd

- name: 'file create step-tickers'
  template:
    src: "{{file_src_step_tickers}}"
    dest: "{{file_dst_step_tickers}}"
    mode: "{{file_mode_step_tickers}}"
    owner: "{{file_owner_step_tickers}}"
    group: "{{file_group_step_tickers}}"
  tags:
    - cronjob
    - ntpd

- name: 'file remove ntpd_check-time'
  file:
    state: 'absent'
    path: "{{file_dst_ntpd_check_time}}"
  tags:
    - cronjob
    - ntpd

- name: 'cron remove ntpd_check-time'
  cron:
    state: 'absent'
    name: "{{cron_name_ntpd_check_time}}"
    user: "{{cron_user_ntpd_check_time}}"
  tags:
    - cronjob
    - ntpd

- name: 'file create ntpd_update-time'
  template:
    src: "{{file_src_ntpd_update_time}}"
    dest: "{{file_dst_ntpd_update_time}}"
    mode: "{{file_mode_ntpd_update_time}}"
    owner: "{{file_owner_ntpd_update_time}}"
    group: "{{file_group_ntpd_update_time}}"
  tags:
    - cronjob
    - ntpd

- name: 'cron create ntpd_update-time'
  cron:
    state: 'present'
    disabled: 'no'
    name: "{{cron_name_ntpd_update_time}}"
    day: "{{cron_day_ntpd_update_time}}"
    hour: "{{cron_hour_ntpd_update_time}}"
    minute: "{{cron_minute_ntpd_update_time}}"
    month: "{{cron_month_ntpd_update_time}}"
    weekday: "{{cron_weekday_ntpd_update_time}}"
    user: "{{cron_user_ntpd_update_time}}"
    job: "{{cron_job_ntpd_update_time}}"
  tags:
    - cronjob
    - ntpd
