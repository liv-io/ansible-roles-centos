---

#-------------------------------------------------------------------------------
# VARIABLES
#-------------------------------------------------------------------------------

- name: 'include variables'
  include_vars: "{{item}}"
  with_first_found:
    - files:
      - "vars/{{ansible_distribution_major_version}}.yml"
      - 'vars/main.yml'
      skip: 'true'
  tags:
    - remove
    - ntpd

#-------------------------------------------------------------------------------
# PACKAGING
#-------------------------------------------------------------------------------

- name: 'package remove ntpd'
  package:
    state: 'absent'
    name: "{{package_name_ntpd}}"
  tags:
    - remove
    - ntpd

#-------------------------------------------------------------------------------
# FILES
#-------------------------------------------------------------------------------

- name: 'cron remove ntpd_check-time'
  cron:
    state: 'absent'
    name: "{{cron_name_ntpd_check_time}}"
    cron_file: "{{cron_cron_file_ntpd_check_time}}"
  tags:
    - remove
    - ntpd

- name: 'file remove ntpd_check-time.sh'
  file:
    state: 'absent'
    path: "{{file_dst_ntpd_check_time_sh}}"
  tags:
    - remove
    - ntpd

- name: 'cron remove ntpd_update-time'
  cron:
    state: 'absent'
    name: "{{cron_name_ntpd_update_time}}"
    cron_file: "{{cron_cron_file_ntpd_update_time}}"
  tags:
    - remove
    - ntpd

- name: 'file remove ntpd_update-time.sh'
  file:
    state: 'absent'
    path: "{{file_dst_ntpd_update_time_sh}}"
  tags:
    - remove
    - ntpd

- name: 'file remove ntpd_script.dir'
  file:
    state: 'absent'
    path: "{{file_dst_ntpd_script_dir}}"
  tags:
    - remove
    - ntpd

- name: 'file remove step-tickers'
  file:
    state: 'absent'
    path: "{{file_dst_step_tickers}}"
  tags:
    - remove
    - ntpd

- name: 'file remove ntp.conf'
  file:
    state: 'absent'
    path: "{{file_dst_ntp_conf}}"
  tags:
    - remove
    - ntpd
