---

#-------------------------------------------------------------------------------
# VARIABLES
#-------------------------------------------------------------------------------

- name: 'include variables'
  include_vars: "{{item}}"
  with_first_found:
    - files:
      - "vars/{{ansible_distribution_major_version}}.yml"
      - 'vars/main.yml'
      skip: 'true'
  tags:
    - config
    - main
    - httpd

#-------------------------------------------------------------------------------
# FILES
#-------------------------------------------------------------------------------

- name: 'template create vhost'
  template:
    src: "{{file_src_vhost}}"
    dest: "{{file_path_vhosts_d}}/{{item.name}}-{{(item.port|int)}}.conf"
    mode: "{{file_mode_vhost}}"
    owner: "{{file_owner_vhost}}"
    group: "{{file_group_vhost}}"
  with_items: "{{httpd_vhost}}"
  notify:
    - 'command check httpd'
    - 'command reload httpd'
  when: not (item.state|match('false|no|remove'))
  tags:
    - files
    - config
    - main
    - httpd

- name: 'file remove vhost'
  file:
    state: 'absent'
    path: "{{file_path_vhosts_d}}/{{item.name}}-{{(item.port|int)}}.conf"
  with_items: "{{httpd_vhost}}"
  notify:
    - 'command check httpd'
    - 'command reload httpd'
  when: (item.state|match('false|no|remove'))
  tags:
    - files
    - config
    - main
    - httpd
