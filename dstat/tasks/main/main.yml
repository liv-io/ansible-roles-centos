---

- name: 'task include install'
  include: install.yml
  when: not (dstat_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - dstat

- name: 'task include remove'
  include: remove.yml
  when: (dstat_state|match('false|no|remove'))
  tags:
    - main
    - dstat
