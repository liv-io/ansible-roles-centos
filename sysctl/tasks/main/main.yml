---

- name: 'task include install'
  import_tasks: install.yml
  when: not (sysctl_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - sysctl

- name: 'task include config'
  import_tasks: config.yml
  when: not (sysctl_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - sysctl
