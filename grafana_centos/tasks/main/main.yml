---

- name: 'task include install'
  import_tasks: install.yml
  when: not (grafana_centos_state is match('false|no|remove|quiesce|inactive'))
  tags:
    - grafana_centos

- name: 'task include enable'
  import_tasks: enable.yml
  when: (grafana_centos_state is match('start|on|enable'))
  tags:
    - grafana_centos

- name: 'task include disable'
  import_tasks: disable.yml
  when: (grafana_centos_state is match('stop|off|disable'))
  tags:
    - grafana_centos

- name: 'task include remove'
  import_tasks: remove.yml
  when: (grafana_centos_state is match('false|no|remove'))
  tags:
    - grafana_centos
