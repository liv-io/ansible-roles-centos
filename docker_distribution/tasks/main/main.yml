---

- name: 'task include install'
  import_tasks: install.yml
  when: not (docker_distribution_state is match('false|no|remove|quiesce|inactive'))
  tags:
    - docker_distribution

- name: 'task include enable'
  import_tasks: enable.yml
  when: (docker_distribution_state is match('start|on|enable'))
  tags:
    - docker_distribution

- name: 'task include disable'
  import_tasks: disable.yml
  when: (docker_distribution_state is match('stop|off|disable'))
  tags:
    - docker_distribution

- name: 'task include remove'
  import_tasks: remove.yml
  when: (docker_distribution_state is match('false|no|remove'))
  tags:
    - docker_distribution
