---

- name: 'task include install'
  import_tasks: install.yml
  when: not (mod_ssl_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - mod_ssl

- name: 'task include enable'
  import_tasks: enable.yml
  when: (mod_ssl_state|match('start|on|enable'))
  tags:
    - main
    - mod_ssl

- name: 'task include disable'
  import_tasks: disable.yml
  when: (mod_ssl_state|match('stop|off|disable'))
  tags:
    - main
    - mod_ssl

- name: 'task include remove'
  import_tasks: remove.yml
  when: (mod_ssl_state|match('false|no|remove'))
  tags:
    - main
    - mod_ssl
