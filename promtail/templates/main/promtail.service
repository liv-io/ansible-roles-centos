{{ansible_managed|comment(decoration='#')}}

[Unit]
Description=Promtail
After=syslog.target network.target

[Service]
User=promtail
Group=promtail
EnvironmentFile={{file_dst_promtail_sysconfig}}
ExecStart={{file_creates_promtail}} $OPTIONS

[Install]
WantedBy=multi-user.target
