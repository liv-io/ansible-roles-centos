---

- name: 'task include install'
  import_tasks: install.yml
  when: not (php_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - php

- name: 'task include enable'
  import_tasks: enable.yml
  when: (php_state|match('start|on|enable'))
  tags:
    - main
    - php

- name: 'task include disable'
  import_tasks: disable.yml
  when: (php_state|match('stop|off|disable'))
  tags:
    - main
    - php

- name: 'task include remove'
  import_tasks: remove.yml
  when: (php_state|match('false|no|remove'))
  tags:
    - main
    - php
