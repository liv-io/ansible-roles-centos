---

#-------------------------------------------------------------------------------
# VARIABLES
#-------------------------------------------------------------------------------

- name: 'include variables'
  include_vars: "{{item}}"
  with_first_found:
    - files:
      - "vars/{{ansible_distribution_major_version}}.yml"
      - 'vars/main.yml'
      skip: 'true'
  tags:
    - enable
    - main
    - mod_security

#-------------------------------------------------------------------------------
# FILES
#-------------------------------------------------------------------------------

- name: 'template create mod_security.conf'
  template:
    src: "{{file_src_mod_security_conf}}"
    dest: "{{file_path_mod_security_conf}}"
    mode: "{{file_mode_mod_security_conf}}"
    owner: "{{file_owner_mod_security_conf}}"
    group: "{{file_group_mod_security_conf}}"
  notify:
    - 'command check httpd'
    - 'command reload httpd'
  tags:
    - files
    - enable
    - main
    - mod_security
