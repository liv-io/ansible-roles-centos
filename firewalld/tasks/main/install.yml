---

#-------------------------------------------------------------------------------
# VARIABLES
#-------------------------------------------------------------------------------

- name: 'include variables'
  include_vars: "{{item}}"
  with_first_found:
    - files:
      - "vars/{{ansible_distribution_major_version}}.yml"
      - 'vars/main.yml'
      errors: 'ignore'
  tags:
    - install
    - firewalld

#-------------------------------------------------------------------------------
# PACKAGING
#-------------------------------------------------------------------------------

- name: 'package install firewalld'
  package:
    state: 'present'
    name: "{{package_name_firewalld}}"
  tags:
    - install
    - firewalld

- name: 'package install firewalld-filesystem'
  package:
    state: 'present'
    name: "{{package_name_firewalld_filesystem}}"
  tags:
    - install
    - firewalld_filesystem
