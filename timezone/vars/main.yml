---

#-------------------------------------------------------------------------------
# COMMANDS
#-------------------------------------------------------------------------------

# get time-zone
command_shell_get_timezone: "timedatectl status | grep '^.*Time zone: ' | awk '{print $3}'"

# set time-zone
command_shell_set_timezone: "timedatectl set-timezone '{{timezone_timezone}}'"
