---

#-------------------------------------------------------------------------------
# COMMANDS
#-------------------------------------------------------------------------------

# get time-zone
command_cmd_get_timezone: "timedatectl status | grep '^.*Time zone: ' | sed 's/^.*Time zone: //'"

# set time-zone
command_cmd_set_timezone: "timedatectl set-timezone '{{timezone_timezone}}'"
