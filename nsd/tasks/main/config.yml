---

#-------------------------------------------------------------------------------
# VARIABLES
#-------------------------------------------------------------------------------

- name: 'include variables'
  include_vars: "{{item}}"
  with_first_found:
    - files:
      - "vars/{{ansible_distribution_major_version}}.yml"
      - 'vars/main.yml'
      skip: 'true'
  tags:
    - config
    - nsd

#-------------------------------------------------------------------------------
# FILES
#-------------------------------------------------------------------------------

- name: 'file create config.zone'
  template:
    src: "{{file_src_config_zone}}"
    dest: "{{file_dst_config_zone}}/{{item.name}}.zone"
    mode: "{{file_mode_config_zone}}"
    owner: "{{file_owner_config_zone}}"
    group: "{{file_group_config_zone}}"
  with_items: "{{nsd_zone}}"
  notify:
    - 'command nsd-checkconf'
    - 'service restart nsd'
  when: not (item.state|default('true') is match('false|no|remove'))
  tags:
    - config
    - nsd

- name: 'file remove config.zone'
  file:
    state: 'absent'
    path: "{{file_dst_config_zone}}/{{item.name}}.zone"
  with_items: "{{nsd_zone}}"
  notify:
    - 'command nsd-checkconf'
    - 'service restart nsd'
  when: (item.state|default('true') is match('false|no|remove'))
  tags:
    - config
    - nsd
