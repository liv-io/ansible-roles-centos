---

#-------------------------------------------------------------------------------
# VARIABLES
#-------------------------------------------------------------------------------

- name: 'include variables'
  include_vars: "{{item}}"
  with_first_found:
    - files:
      - "vars/{{ansible_distribution_major_version}}.yml"
      - 'vars/main.yml'
      errors: 'ignore'
  tags:
    - cronjob
    - chrony

#-------------------------------------------------------------------------------
# SYSTEM
#-------------------------------------------------------------------------------

- name: 'service stop chronyd'
  service:
    state: 'stopped'
    enabled: 'no'
    name: "{{service_name_chronyd}}"
  tags:
    - cronjob
    - chrony

#-------------------------------------------------------------------------------
# FILES
#-------------------------------------------------------------------------------

- name: 'file create chrony.conf'
  template:
    src: "{{file_src_chrony_conf}}"
    dest: "{{file_dst_chrony_conf}}"
    mode: "{{file_mode_chrony_conf}}"
    owner: "{{file_owner_chrony_conf}}"
    group: "{{file_group_chrony_conf}}"
  tags:
    - cronjob
    - chrony

- name: 'cron remove chrony_check-time'
  cron:
    state: 'absent'
    name: "{{cron_name_chrony_check_time}}"
    cron_file: "{{cron_cron_file_chrony_check_time}}"
    user: "{{cron_user_chrony_check_time}}"
  tags:
    - cronjob
    - chrony

- name: 'file remove chrony_check-time.sh'
  file:
    state: 'absent'
    path: "{{file_dst_chrony_check_time_sh}}"
  tags:
    - cronjob
    - chrony

- name: 'file create chrony_update-time.sh'
  template:
    src: "{{file_src_chrony_update_time_sh}}"
    dest: "{{file_dst_chrony_update_time_sh}}"
    mode: "{{file_mode_chrony_update_time_sh}}"
    owner: "{{file_owner_chrony_update_time_sh}}"
    group: "{{file_group_chrony_update_time_sh}}"
  tags:
    - cronjob
    - chrony

- name: 'cron create chrony_update-time'
  cron:
    state: 'present'
    disabled: 'no'
    name: "{{cron_name_chrony_update_time}}"
    cron_file: "{{cron_cron_file_chrony_update_time}}"
    minute: "{{cron_minute_chrony_update_time}}"
    user: "{{cron_user_chrony_update_time}}"
    job: "{{cron_job_chrony_update_time}}"
  tags:
    - cronjob
    - chrony
