---

#-------------------------------------------------------------------------------
# VARIABLES
#-------------------------------------------------------------------------------

- name: 'include variables'
  include_vars: "{{item}}"
  with_first_found:
    - files:
      - "vars/{{ansible_distribution_major_version}}.yml"
      - 'vars/main.yml'
      skip: 'true'
  tags:
    - remove
    - docker_compose

#-------------------------------------------------------------------------------
# PACKAGING
#-------------------------------------------------------------------------------

- name: 'package remove docker-compose'
  package:
    state: 'absent'
    name: "{{package_name_docker_compose}}"
  tags:
    - remove
    - docker_compose

- name: 'pip remove docker-compose'
  pip:
    state: 'absent'
    name: "{{pip_name_docker_compose}}"
  tags:
    - install
    - docker_compose
